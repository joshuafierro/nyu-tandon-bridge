#include <iostream>
#include <string>
#include <vector>
using namespace std;

int* findMissing(int arr[], int n, int& resArrSize);

int main(){

    return 0;
}

/*
cCreates and returns a new array, that contains all the numbers in range {0, 1, 2, ... , n} 
that are not in arr. The function also updates the output parameter, resArrSize, 
with the logical size of the new array that was created.
*/

int* findMissing(int arr[], int n, int& resArrSize){
    
}